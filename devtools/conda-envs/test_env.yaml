name: test
channels:
  - conda-forge

  - defaults
dependencies:
  # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - numpy
  - matplotlib

    # Pip-only installs
  #- pip:
  #  - codecov
